import React, { useState, useEffect } from "react"; 

function Typewriter({ textArray, speed = 100, delay = 1000}) { 
    const [currentText, setCurrentText] = useState(0); //rerenders component when setCurrentText is called
    const [lineIndex, ]
    
}